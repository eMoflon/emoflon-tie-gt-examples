import "platform:/plugin/test2/model/Test2.ecore"
import "http://www.eclipse.org/emf/2002/Ecore"

using "test.gt"

package src.test

class Box {
	operation matchABox():void{
	    this:Box
		p:Partition
		invoke matchPartition(matchPartition.b = this, matchPartition.partition = p)
		invoke addPartition(addPartition.b = this)
		invoke removePartition(removePartition.b= this)
	}

}
